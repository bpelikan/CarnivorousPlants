@using CarnivorousPlants.Models.ImageViewModel
@model ChangeImageTagViewModel

@{
    ViewData["Title"] = "Change Image Tag";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h1>Change Image Tag</h1>


<form asp-controller="Image" asp-action="ChangeImageTag" asp-route-projectId="@Model.ProjectId" asp-route-imageId="@Model.Image.Id" method="post" class="form-horizontal" role="form">
    <div asp-validation-summary="All" class="text-danger"></div>
    
    @*<div class="form-group">
        <label asp-for="StartDate" class="col-md-2 control-label"></label>
        <div class="col-md-10">
            <input asp-for="StartDate" class="form-control" />
            <span asp-validation-for="StartDate" class="text-danger"></span>
        </div>
    </div>*@


    <div>
        <img src="@Model.Image.ThumbnailUri" />
        <div>
            <b>@Html.DisplayNameFor(model => model.TagId)</b>
            <select asp-for="TagId" class="form-control" asp-items="@Model.TagsSelectList">
                <option selected value="">--- SELECT ---</option>
            </select>
        </div>
    </div>
    <hr />
    

    <div class="form-group">
        <div>
            <input type="submit" class="btn btn-primary btn-block" value="Change" />
            <a class="btn btn-sm btn-block btn-outline-primary" asp-controller="Project" asp-action="Details" asp-route-projectId="@Model.ProjectId">Back</a>
        </div>
    </div>
</form>
